# Test workflow for pre-commit-check-reusable.yml
name: Test Pre-commit Check

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  test-pre-commit:
    name: Test Pre-commit Execution
    uses: maxrantil/.github/.github/workflows/pre-commit-check-reusable.yml@feat/issue-3-explicit-permissions
    with:
      python-version: '3.x'
      base-branch: master
      run-on-all-files: false
