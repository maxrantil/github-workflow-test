# Test workflow for pre-commit-check-reusable.yml
name: Test Pre-commit Check

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  test-pre-commit:
    name: Test Pre-commit Execution (Testing Caching)
    uses: maxrantil/.github/.github/workflows/pre-commit-check-reusable.yml@feat/issue-4-workflow-caching
    with:
      python-version: '3.x'
      base-branch: master
      run-on-all-files: false
